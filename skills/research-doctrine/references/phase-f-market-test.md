# Phase F - Reality-test Template

Chat: paste the contents of `SKILL.md` (below the YAML frontmatter) into your system prompt before starting this chat, or use the skill if installed. Agentic harness: invoke the skill and pass the Phase E artifact's file path as the only inherited input.

Phase F only exists when the Phase E decision is a bet. A fact-state update ends at E ("Terminal - no Phase F"). Pick the variant that matches the decision:

- **Market test** - the decision bets on demand: prospects, pilots, offers.
- **Verification test** - the decision turns on a fact only the real world can confirm: the paid consultation, the official enquiry, the filed pre-application.

---

```
MODE: Strategic
PHASE: F-Reality-test
```

Tool use is permitted for test logistics only (building the prospect or contact list, finding the booking page or filing portal), not for reopening the research.

In Phase F the failure-mode obligation is discharged by the Phase D and E artifacts: carry the kill thresholds into the failure metric; do not regenerate counter-arguments or the pre-mortem.

## Phase E artifact
[Paste the Phase E artifact here, or give its file path.]

## Reality-test instructions

Plan a real-world test of the Phase E decision. You are NOT executing the test - you are designing it.

Output structure:

1. Test hypothesis (the specific thing being validated)
2. Test method (market test: real conversations, prototype, paid pilot. Verification test: the consult, the enquiry, the filing)
3. Success metric (concrete, testable, time-bounded)
4. Failure metric (what would invalidate the decision)
5. Sample size and timeline (market test), or the booking/filing step and date (verification test)
6. Prospect or contact list (if applicable)

The doctrine forbids substituting more research for the real-world test. The output is a plan you execute outside the chat.

This artifact is terminal. Replace the last two standard sections with:

- **Execution plan owner:** who runs the test and by when
- **Re-entry condition:** what result sends this back to Phase A or B
