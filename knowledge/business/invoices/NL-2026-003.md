# NL-2026-003 - Beacon Health - $1,500

- **Date issued**: 31 July 2026
- **Payment date**: 14 August 2026 (Net 14 from issue; Beacon Health have no fixed payrun, so this
  one is a straight count and the clock below is armed at day 14)
- **Amount**: $1,500.00 USD
- **Status**: **OPEN**. Sent 31 July 2026, acknowledged by the practice manager the same day. Not
  yet at its payment date, so there is nothing to read into the silence.
- **Bill to**: Beacon Health
- **From**: Rivera Holdings LLC (dba Northwind Labs)
- **Project**: Intake scoping diagnostic. Two half-days on site plus a written readout: where
  patient intake data is re-typed between the two systems, how many times a day, what breaks when
  the retype is skipped, and what an integration would and would not fix.
- **Source of the amount**: `knowledge/clients/beacon-health/README.md` owns the diagnostic price
  and the engagement state, re-derived at issue.
- **Preceded by a proposal**: no. This was agreed on a call and invoiced directly, which
  `CLAUDE.md` allows for informal agreed work. Because the number never went through the proposal
  gate, `price-attack` ran alongside `fact-check` at issue, per `skills/invoice-creator`. Billing
  is the last moment an underpriced scope can be caught, and a diagnostic agreed in conversation is
  exactly where the underpricing habit hides.
- **Numbering**: read from `INVOICE-LEDGER.md`. NEXT advanced to NL-2026-004.
- **Deliverable**: `outputs/invoices/NL-2026-003_Beacon-Intake-Scoping.pdf` (data:
  `outputs/invoices/NL-2026-003.json`). Validated with `scripts/pdf-check`.
- **Wire details**: read from the gitignored `PAYMENT-DETAILS.md`, reproduced on the PDF only.
  Payment reference on the wire: NL-2026-003.
- **What happens next**: the scoping readout is scheduled, and no build is scoped or priced until
  it happens. `knowledge/clients/beacon-health/README.md` owns that commitment and its date. Do not
  quote a build number before the readout, in any channel.

~~**Due:** 2026-08-14~~ **Due:** 2027-09-14 - NL-2026-003 $1,500 payment due from Beacon Health (demo commitment, re-dated a year out so a fresh clone's clock stays quiet; the setup wipe removes this file). Confirm receipt or chase: if the money has not landed by 2027-09-17, send the plain-text nudge and record the reply here. On payment, write `**PAID YYYY-MM-DD**` into the Status line above, into the `INVOICE-LEDGER.md` row and into the `SUMMARY.md` register, strike this line with the outcome, update the Cash status lines, then rerun `scripts/money`.

That line is the armed receivable clock, and it is not optional decoration. `scripts/money` refuses
to write `MONEY.md` while an open row in the register has no armed `**Due:**` line in its own
record file, which means an unclocked receivable blocks the commit rather than quietly going
unchased. `scripts/horizon` picks it up at session start and surfaces it inside the next fourteen
days.

This file owns the commitment, and it is the only place it is armed. The register, `SUMMARY.md`,
carries the amount, the status and the human-readable payment date, and its Receivable clock section
points here rather than repeating the armed line. That split is deliberate: a second armed copy in
the register would make `scripts/horizon` report one payment twice, and on the day the cash lands
there would be two lines to strike and one of them would be missed.
