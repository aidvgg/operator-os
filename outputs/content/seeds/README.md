# outputs/content/seeds/

Output folder for the `harvest` skill (`skills/harvest/SKILL.md`): one `YYYY-MM-DD-<slug>.md` per
seed, each carrying an X-voice hook, a pillar, the receipt that earned it, and a self-rating.
Drafts only. Nothing here is posted by an agent; the operator curates. Empty on a fresh clone.
