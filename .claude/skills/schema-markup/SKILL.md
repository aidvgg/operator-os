---
name: schema-markup
description: "Structured data and JSON-LD schema markup for search engines and AI answer surfaces, including validation and common markup mistakes."
---

<!-- GENERATED by scripts/sync-agent-adapters from ai/AGENT_ROUTES.json. DO NOT EDIT. -->
# schema-markup skill adapter

Canonical skill: `skills/schema-markup/SKILL.md`. Read that file in full before acting, then follow it exactly.

This adapter exists so a runtime that discovers repository skills under `.agents/skills/` or `.claude/skills/` can reach a skill whose canonical copy lives in `skills/`. It owns no procedure. Do not restate, shorten, or improvise the canonical steps, and do not treat this summary as the skill.

Read `CLAUDE.md` before executing. It is the shared operating contract and it outranks this skill on precedence, voice, pricing policy, and what counts as a sanctioned write. The canonical skill wins on execution mechanics.

Vendored reference material, not this repository's doctrine. Provenance, licences, and the deliberate skip list live in `skills/VENDORED.md`. This repository's voice, pricing, and precedence rules override anything this file models, including the em-dash copy style several of these files demonstrate, which is banned in every artifact this repo produces. It carries no `knowledge/` write licence and no send authority.

## Maintenance

Do not edit this file directly. Change `skills/schema-markup/SKILL.md` for procedure or `ai/AGENT_ROUTES.json` for routing, then run `scripts/sync-agent-adapters --write`. `scripts/repo-doctor` blocks stale generated surfaces.
