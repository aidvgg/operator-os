---
description: Set up Operator OS for a new operator - first-run steps, a plain-language interview, the rebrand, the demo wipe, verification, and the first saved snapshot. Never reads bank or key files, never pushes.
---
Run the onboard procedure for: $ARGUMENTS

Read `skills/onboard/SKILL.md` in full and follow it exactly. That file owns the procedure and is
the single owner shared with the Codex adapter at `.agents/skills/onboard/SKILL.md`. Do not
restate it here, do not improvise a shorter version.

Speak to the person in plain words, one question at a time. Report what changed, what they still
do by hand, the `TODO(onboard)` lines, and the five first-week sentences, in plain text.
